(ns ericdallo.nrepl-graalvm-sample-test
  (:require [clojure.test :refer :all]
            [ericdallo.nrepl-graalvm-sample :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
